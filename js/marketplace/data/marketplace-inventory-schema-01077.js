// 01077 · Inventory reservation / stock commit schema.
export const MARKETPLACE_INVENTORY_STAGE_01077='01077';
export const INVENTORY_RESERVATION_STATUSES_01077=['active','committed','released','expired'];
export const INVENTORY_ITEM_STATUSES_01077=['active','committed','released','expired','restocked'];
const str=v=>String(v??'').trim();
const num=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f;
const now=()=>new Date().toISOString();
function uid(prefix){try{return `${prefix}_${crypto.randomUUID().replace(/-/g,'').slice(0,24)}`;}catch{return `${prefix}_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,10)}`;}}
export function createInventoryReservationItem01077(input={}){return {id:str(input.id)||uid('invitem'),sellerOrderId:str(input.sellerOrderId),orderItemId:str(input.orderItemId),sellerOfferId:str(input.sellerOfferId),sellerProfileId:str(input.sellerProfileId),storeId:str(input.storeId),sourceProductId:str(input.sourceProductId),quantity:Math.max(1,Math.floor(num(input.quantity,1))),status:INVENTORY_ITEM_STATUSES_01077.includes(str(input.status))?str(input.status):'active',createdAt:str(input.createdAt)||now(),updatedAt:str(input.updatedAt)||now()};}
export function createInventoryReservation01077(input={}){const t=now();return {id:str(input.id)||uid('invres'),marketplaceOrderId:str(input.marketplaceOrderId),cartId:str(input.cartId),paymentMethod:str(input.paymentMethod)||'cod',status:INVENTORY_RESERVATION_STATUSES_01077.includes(str(input.status))?str(input.status):'active',expiresAt:str(input.expiresAt)||new Date(Date.now()+15*60*1000).toISOString(),committedAt:str(input.committedAt),releasedAt:str(input.releasedAt),releaseReason:str(input.releaseReason),items:Array.isArray(input.items)?input.items.map(createInventoryReservationItem01077):[],createdAt:str(input.createdAt)||t,updatedAt:str(input.updatedAt)||t};}
export function createInventoryLedgerEntry01077(input={}){return {id:str(input.id)||uid('invlog'),sellerOfferId:str(input.sellerOfferId),storeId:str(input.storeId),reservationId:str(input.reservationId),marketplaceOrderId:str(input.marketplaceOrderId),sellerOrderId:str(input.sellerOrderId),entryType:['reserve','release','expire','commit','restock','adjustment'].includes(str(input.entryType))?str(input.entryType):'adjustment',quantity:num(input.quantity),stockAfter:input.stockAfter==null?null:num(input.stockAfter),metadata:input.metadata&&typeof input.metadata==='object'&&!Array.isArray(input.metadata)?{...input.metadata}:{},createdAt:str(input.createdAt)||now()};}
export function summarizeInventoryOffer01077(input={}){const physicalStock=Math.max(0,num(input.physicalStock,input.stock)),reservedStock=Math.max(0,num(input.reservedStock)),committedStock=Math.max(0,num(input.committedStock)),availability=str(input.availability)||'in-stock';return {...input,physicalStock,reservedStock,committedStock,availableStock:availability==='preorder'?99:Math.max(0,physicalStock-reservedStock)};}
